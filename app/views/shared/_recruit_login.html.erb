<div class="container pt-5">
  <div class="row">
    <div class="display-t js-fullheight">
      <div class="display-tc js-fullheight animate-box" data-animate-effect="fadeIn">
        <div class="row recruit-login pt-5">
          <h3 class="title-form text-center">【採用担当者様　専用ログインフォーム】</h3>
          <h4 class="description-form mb-3 text-center">下記の「専用ログイン」ボタンを押していただければ、<br>すぐに専用アカウントでログインが可能です。</h4>
          <div class="col-md-6 col-md-offset-3 mt-3">
            <%= form_for(:session, url: login_path, local: true) do |f| %>
              <div class="form-group">
                <%= f.label 'メールアドレス' %>
                <%= f.email_field :email, class: 'form-control', id: 'recruit_user_email', value: 'recruit@example.com' %>
              </div>
              <div class="form-group">
                <%= f.label 'パスワード' %>
                <%= f.password_field :password, class: 'form-control', id: 'recruit_user_password', value: 'password' %>
              </div>
          </div>
          <div style="text-align:center;">
          <%= f.submit '専用ログイン', class: 'btn-recruit btn-primary'  %>
          </div>
            <% end %>
            <div class="recruit-account text-center">
              <p>＜採用担当者様　専用アカウント＞</p>
                <p>メールアドレス：recruit@example.com</p>
                <p>パスワード：password</p>
                <p>※フォームに予め入力済</p>
            </div>
        </div>
      </div>
    </div>
  </div>
</div>